import { APP_NAME } from "@edenomics/constants/app";

export const getMetaDescription = (description: string) => {
  return `${description} ${APP_NAME} is your all-in-one personal finance companion. Track expenses, analyze spending habits, allocate monthly budgets, and make smarter financial decisions. Take control of your money with ease.`;
};
